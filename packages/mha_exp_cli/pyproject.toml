[project]
name = "mha-exp-cli"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "mha-exp-common",
]

[project.scripts]
mha-exp = "mha_exp_cli.__main__:main"

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"
