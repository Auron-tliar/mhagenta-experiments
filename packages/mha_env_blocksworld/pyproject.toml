[project]
name = "mha-env-blocksworld"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "gymnasium~=1.3.0",
    "imageio[ffmpeg]>=2.37.3",
    "numpy~=2.4.4",
    "pygame~=2.6.1",
    "unified-planning~=1.3.0",
]

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"
